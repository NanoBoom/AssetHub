# ============================================
# AssetHub Docker 环境变量配置模板
# ============================================
# 复制此文件为 .env.docker 并根据需要修改
# 使用方法: docker-compose --env-file .env.docker up
#
# ⚠️ 安全警告:
# 此配置用于开发环境，包含默认弱密码
# 生产环境必须修改所有密码和密钥！

# ============================================
# 应用配置
# ============================================
APP_NAME=AssetHub
APP_PORT=8080
APP_ENV=development

# ============================================
# 数据库 (PostgreSQL)
# ============================================
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=assethub
DB_SSLMODE=disable

# ============================================
# Redis
# ============================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=2

# ============================================
# 日志
# ============================================
LOG_LEVEL=debug

# ============================================
# 存储配置
# ============================================
# 存储类型: s3 (AWS S3/MinIO), oss (阿里云 OSS), local
STORAGE_TYPE=s3

# === S3 存储 (AWS S3 - 优先选择) ===
# 使用 AWS S3 时，留空 S3_ENDPOINT，设置 S3_USE_PATH_STYLE=false
S3_REGION=us-east-1
S3_BUCKET=your-bucket-name
S3_ACCESS_KEY_ID=your_aws_access_key_id
S3_SECRET_ACCESS_KEY=your_aws_secret_access_key
S3_ENDPOINT=  # AWS S3 留空
S3_USE_PATH_STYLE=false

# === S3 存储 (本地 MinIO 集群) ===
# 如果使用外部 MinIO 集群，取消注释并修改以下配置：
# S3_REGION=us-east-1
# S3_BUCKET=assethub-files
# S3_ACCESS_KEY_ID=minioadmin
# S3_SECRET_ACCESS_KEY=minioadmin
# S3_ENDPOINT=http://localhost:9000
# S3_USE_PATH_STYLE=true

# === OSS 存储 (阿里云 OSS) ===
# OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
# OSS_BUCKET=your-bucket-name
# OSS_ACCESS_KEY_ID=your_access_key_id
# OSS_ACCESS_KEY_SECRET=your_access_key_secret

# === 本地存储 ===
# LOCAL_BASE_PATH=./storage
